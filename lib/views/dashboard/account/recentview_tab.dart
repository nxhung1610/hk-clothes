import 'package:flutter/material.dart';

class RecentViewTab extends StatelessWidget {
  const RecentViewTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
